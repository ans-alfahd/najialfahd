# najialfahd

نظام بسيط لإدارة الكتب والوثائق الصادرة والواردة.

## التشغيل

```bash
pip install -r requirements.txt
python app.py
```

يفتح التطبيق على `http://127.0.0.1:5000/` ويتيح إضافة وتعديل وحذف وطباعة وتصدير واستيراد البيانات.
